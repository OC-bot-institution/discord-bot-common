[project]
name = "discord-bot-common"
version = "0.1.4"
description = "Common utilities for Discord bots"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["bot_common*"]

[tool.setuptools.package-data]
bot_common = ["data/*.json"]